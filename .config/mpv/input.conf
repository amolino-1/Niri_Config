# Press g to toggle playlist shuffle on/off
g playlist-shuffle; cycle shuffle

# Apply VR profile, then cycle VR monitor presets
F9 apply-profile vr; script-message vr-cycle

# Optional: restore normal playback settings
Shift+F9 apply-profile vr restore
